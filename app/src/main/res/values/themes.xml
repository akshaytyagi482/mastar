<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Editor is always dark: video previews read best on black, and dark
         pixels save power on the AMOLED panels common in Indian mid-rangers. -->
    <style name="Theme.Mastar" parent="android:Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
